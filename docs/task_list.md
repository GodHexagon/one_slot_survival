# タスクリスト

## 長期計画

1. 完了✅|デバッグ実行高速化施策を実施する
    * Gradle Daemon有効化
    * JVMメモリ最適化（Gradle及びrunClientプロセス）
    * 詳細：[ビルド速度上昇施策](./build-speed-up.md)
1. 完了✅|プレイヤーごとに機能の有効性を変更できるようにする。
    * 管理者権限を持つ者だけが実行できる、管理者アクションの一つとする。
    * クライアントサイドで取得可能・セーブデータに永続化・ワールドごとで保存を実現するため、**Attributeシステム**で管理する。
   1. 完了✅|Attribute方式で永続化
       * 実装: ModAttributes.MOD_ENABLED による自動永続化・自動クライアント同期
       * コマンド: `/oneslot <players> enable|disable|toggle|status` および `/oneslot status`
   2. ~~コマンド方式でクライアントからMOD有効性を取得~~ ロールバック済み
   3. ~~同期要求パケットでサーバー側から同期~~ ロールバック済み
   4. 完了✅|共通データパースロジックで、MOD有効性を取得
1. プレイヤーのホットバーは１スロットだけ（これをメインハンドと定義する）になり、それ以外のインベントリとホットバー計３５スロットを使用禁止する。
    * メインハンドがいっぱいになった場合、バニラでインベントリがいっぱいになっときみたいに、新たにアイテムを拾うことができなくなる。
    * メインハンド以外のスロットを保持（ホットバー選択）できない。
    * バニラの画面コンテナ、プレイヤーデータスロット、ホットバー表示、ホットバー変更入力などを直接変更して実装する。
   1. 完了✅|ホットバーの見た目変更
       * 実装: GuiMixin で Matrix3x2fStack による描画位置オフセット
       * 一番左のスロット（スロット0）が画面中央に表示されるように調整
       * MOD有効化プレイヤーのみに適用（PlayerModValidity.isEnabled チェック）
   2. 完了✅|ゲーム中ホットバー選択変更拒否 -> 過去のバージョンを掘り出して実装
   3. 完了✅|ホットバーの見た目変更（リソース変更）
   4. 完了✅|カスタムインベントリを適用させる（テストの背景が表示される）
   3. 完了✅|スロットに収まった各アイテムの外観が表示されるのを防ぐ
   4. 完了✅|各アイテムのツールチップを防ぐ
   5. 完了✅|各アイテムのクリックを防ぐ
   6. 完了✅|各アイテムのその他の操作を防ぐ
   7. 完了✅|クリエイティブインベントリと比較・デバッグ
       * RestrictedSlotWrapperとRestrictedInventoryScreenを実装
       * スロットインデックスの問題を特定・修正し、正常動作を確認
   8. ~~インベントリ内で禁止操作を制限~~ バリアアイテム方式で解決
   5. バリアアイテムで拾得禁止＆見えないところにアイテムが吸われる問題を解決
1. 実現可能性を探る
    * インベントリからアクセス可能なロールスロット
    * ホットバー選択できるロールスロット
    * インベントリにjava.util.Random使っておみくじ <- 「一般プレイヤーによって自身のロールを変更できるようにする」
1. チェストとかも見た目を変更する
    * CreativeModeInventoryScreenを模倣したヘルパークラスを作成する作戦
      * CreativeModeInventoryScreenの完全コピー -> ヘルパーメソッド（public化）でアクセス可能に -> いらない処理を削る・カスタマイズ
1. プレイヤーはロールスロットを利用できるようにする。
    * プレイヤーはメインハンドのほかに、ロールスロットがホットバーとしてあり、ホットバー選択できる。
        * メインスロットの右側に３つある。
        * つるはししか入れられない。
        * ゲーム中は通常のホットバーと同じ挙動をする。
        * インベントリ中では、防具スロットのように専用スロットとしての挙動をする。
        * ドロップしたつるはしを拾う際は普通に入る。
        * オフハンドからつるはし以外をFキーで直接入れようしても、なにも起こらない。
1. 右から１つ目と２つ目のロールスロットはシャベルしか入らないようにする。
1. 各プレイヤーのロールを変更する管理者アクションを追加。
1. プレイヤーに割り当てられたメインロールによって、ロールスロットに入れることができるアイテムが変わるようにする。
    * 今までのロールスロットはMinerのものとし、新たにWarriorを追加する。Unallocatedはロールスロットが許可されない。
1. 管理者アクションをGUI操作できるようにする。
1. メインロールに紐づくEXPとレベルアップがあり、これによりロールスロットをカスタムできるようにする。
1. ワールドのロール変更可能性オプションを変更する管理者アクションを追加。
1. 一般プレイヤーによって自身のロールを変更できるようにする。
    * インベントリに、ロール変更メニューが追加される。
1. 全プレイヤーロール一斉割り当ての管理者アクションを追加する。
1. ワールドの初期ロール割り当て方式オプションを変更する管理者アクションを追加。
1. それ以外の管理者アクションを追加。
1. メインロールの種類を増やす。
1. サブロールを追加する。
