# タスクリスト

## 長期計画

1. 完了✅|デバッグ実行高速化施策を実施する
    * Gradle Daemon有効化
    * JVMメモリ最適化（Gradle及びrunClientプロセス）
    * 詳細：[ビルド速度上昇施策](./build-speed-up.md)
1. 完了✅|プレイヤーごとに機能の有効性を変更できるようにする。
    * 管理者権限を持つ者だけが実行できる、管理者アクションの一つとする。
    * クライアントサイドで取得可能・セーブデータに永続化・ワールドごとで保存を実現するため、**Attributeシステム**で管理する。
   1. 完了✅|Attribute方式で永続化
       * 実装: ModAttributes.MOD_ENABLED による自動永続化・自動クライアント同期
       * コマンド: `/oneslot <players> enable|disable|toggle|status` および `/oneslot status`
   2. ~~コマンド方式でクライアントからMOD有効性を取得~~ ロールバック済み
   3. ~~同期要求パケットでサーバー側から同期~~ ロールバック済み
   4. 完了✅|共通データパースロジックで、MOD有効性を取得
1. 完了✅|プレイヤーのホットバーは１スロットだけ（これをメインハンドと定義する）になり、それ以外のインベントリとホットバー計３５スロットを使用禁止する。
    * メインハンドがいっぱいになった場合、バニラでインベントリがいっぱいになっときみたいに、新たにアイテムを拾うことができなくなる。
    * メインハンド以外のスロットを保持（ホットバー選択）できない。
    * バニラの画面コンテナ、プレイヤーデータスロット、ホットバー表示、ホットバー変更入力などを直接変更して実装する。
   1. 完了✅|ホットバーの見た目変更
       * 実装: GuiMixin で Matrix3x2fStack による描画位置オフセット
       * 一番左のスロット（スロット0）が画面中央に表示されるように調整
       * MOD有効化プレイヤーのみに適用（PlayerModValidity.isEnabled チェック）
   2. 完了✅|ゲーム中ホットバー選択変更拒否 -> 過去のバージョンを掘り出して実装
   3. 完了✅|ホットバーの見た目変更（リソース変更）
   4. 完了✅|カスタムインベントリを適用させる（テストの背景が表示される）
   3. 完了✅|スロットに収まった各アイテムの外観が表示されるのを防ぐ
   4. 完了✅|各アイテムのツールチップを防ぐ
   5. 完了✅|各アイテムのクリックを防ぐ
   6. 完了✅|各アイテムのその他の操作を防ぐ
   7. 完了✅|クリエイティブインベントリと比較・デバッグ
       * RestrictedSlotWrapperとRestrictedInventoryScreenを実装
       * スロットインデックスの問題を特定・修正し、正常動作を確認
   8. ~~インベントリ内で禁止操作を制限~~ バリアアイテム方式で解決
   5. 完了✅|バリアアイテムで拾得禁止＆見えないところにアイテムが吸われる問題を解決
1. 完了✅|実現可能性を探る
    * 完了✅|インベントリからアクセス可能なロールスロット
      * 拾得制限なし、ラグ感は見つからなかった
    * 完了✅|ホットバー選択できるロールスロット
    * ~~スロットバリアを持っても素手と同じにする~~ AbstractContainerMenu.doClick and Inventory挿入方式にしたため不要
    * ~~インベントリにjava.util.Random使っておみくじ <- 「一般プレイヤーによって自身のロールを変更できるようにする」~~ 後でやる
1. 完了✅|修正
   * 完了✅|スロットバリアのツールチップが表示される問題を修正
   * 完了✅|全ソースコードの英語コメントを日本語に翻訳・コードとの整合性を確認
   * 完了✅|全ソースコードを再レビュー
1. 完了✅|プレイヤーはロールスロットを利用できるようにする。
    * プレイヤーはメインハンドのほかに、ロールスロットがホットバーとしてあり、ホットバー選択できる。
        * メインスロットの右側に３つある。
        * つるはししか入れられない。
        * ゲーム中は通常のホットバーと同じ挙動をする。
        * インベントリ中では、防具スロットのように専用スロットとしての挙動をする。
        * ~~ドロップしたつるはしを拾う際は普通に入る。~~ 難しそうなので後でする
        * オフハンドからつるはし以外をFキーで直接入れようしても、なにも起こらない。
   1. 完了✅|スロットバリア、ホットバーの見た目、ホットバー選択制御を４スロット分にする
   2. 完了✅|Inventory挿入で拾得制限 ~~＆その他（バニラのメソッド全部レビュー）~~
   3. 完了✅|死ぬとスロットバリアが残らない問題を修正
   4. 完了✅|インベントリの見た目を４スロット分にする
   5. 完了✅|AbstractContainerMenu.clickedインジェクション＆roledPlayerDoClickで本来の処理をトレース＆ClickTypeが何を意味するかをデバッグログ出力で確認
   6. 完了✅|isRoleSlot(int)を持つインターフェイスを定義・AbstractContainerMenu以下全てに実装＆isRoleSlotが正しいかをデバッグ出力で確認
   7. 完了✅|RoledPlayerDoClickをカスタマイズし、ロールスロットに特定アイテム種類のみ配置可能に制限
   8. 完了✅|毎ティックスロットバリア配布処理がされる問題を修正する
   9. 完了✅|クイック移動のときに普通にロールスロットに入る問題を修正する
   10. 完了✅|オフハンド制限onLivingSwapHandItems方式Integratedでテスト
   11. 完了✅|オフハンド制限onLivingSwapHandItems方式Dedicatedでテスト
   12. 完了✅|ゲーム中にオフハンドからロールスロットへの移動を制限する
   13. 完了✅|エージェントドキュメントWeb検索おろそかにしすぎる問題を解決
   14. 完了✅|拾得制限の別の方式を探る
        * Inventory.getFreeSlotインジェクション方式をやめ、Inventory.addインジェクション方式に切り替え
        * ついでに、「ドロップしたつるはしを拾う際は普通に入る」も実装
   15. 完了✅|スロットバリアを消してもよさそうか考える
        * スロットバリアを消すと見た目でスロット無効化状態がわかりにくくユーザー体験が低下する。とりあえずそのままにして、先に3種のロールを実装するのが先決と判断した。
1. 完了✅|インベントリとホットバーの見た目を良くする。
1. 完了✅|ロールスロットを強化する
    * 左から、つるはし、斧、シャベル
1. 完了✅|ホットバーの位置を見直す
1. 完了✅|各プレイヤーのロールを変更する管理者アクションを追加。
1. 完了✅|プレイヤーに割り当てられたメインロールによって、ロールスロットに入れることができるアイテムが変わるようにする。
    * 今までのロールスロットはMinerのものとし、新たにWarriorを追加する。Unallocatedはロールスロットが許可されない。
    * Warrior: 剣、盾または火打ち石と打ち金、トライデントまたはメイス
1. 完了✅|レベルアップでロールスロットが自動で解放されるようにする
    * 解放状況に応じてインベントリとホットバーの見た目が変化。
    1. 完了✅|残り経験値のアトリビュート＆コマンド
    1. 完了✅|レベルのアトリビュート＆コマンド
    1. 完了✅|プレイ操作で経験値を獲得できるようにする
        * 直近の経験値獲得ログを表示する一般コマンド
    1. 完了✅|レベルが上がるイベントを実装
    1. 完了✅|レベルによるスロット解放を実装
    1. 完了✅|レベルが上がるとメッセージ表示
    1. 完了✅|管理者コマンド整備
    1. 完了✅|レベルと経験値確認一般コマンド
    1. 完了✅|ホットバー外観
    1. 完了✅|ホットバー選択制限
    1. 完了✅|コマンドのエイリアス追加する
    1. 完了✅|バージョン番号を更新する
1. 完了✅|Inventoryデバッグ
    1. 完了✅|getSlotWithRemainingSpaceをインジェクション化
    1. 完了✅|getFreeSlotを代替用途でインジェクション化
    1. 完了✅|one_slot_survival$getFreeSlotはone_slot_survival$roledPlayerGetFreeSlotとして、Inventoryで呼び出されるすべてのか所で呼び出すよう変更
1. 完了✅|ロール未割当でもホットバーが拡張されているように見える問題を修正する
1. 完了✅|新規ログイン時にデフォルト設定を割り当てるようにする
1. 完了✅|経験値設定調整
1. 完了✅|"oss"エイリアスを見直す
1. 完了✅|状態表示コマンドを"oneslot status"及び"oneslot"に変更する
1. 完了✅|コマンドによる経験値初期化がレベルによる変化に適応していない問題を修正する
1. 完了✅|バンドルが使用できない問題を修正する
1. 完了✅|RestrictedInventoryScreenをロールスロットに適応させる
    * リファクタリング
    * アイテム枠とアイテム表示が変化する
1. 一般プレイヤーによって自身のロールを変更できるようにする
    * ロール変更はスロットの解放状況をリセットしレベルをリセットしない
    1. 完了✅|ロール変更可能性設定コマンドを実装
    1. ~~ワールド設定をデフォルトに戻すコマンドを実装~~ ワールドオプションデフォルトシステムは後で実装するため無し
    1. ロール変更一般プレイヤーコマンドを実装: `oneslot changerole to <Role> from <Current Role>`
    1. `oneslot status`コマンドの表示を見直し
    1. 情報閲覧一般コマンド作成
        - `role list`: ロール一覧
        - `role detail [<Role>]`: 特定のロールの情報
        - `slot detail <Slot> [role <Role>]`: 特定のスロットの情報
    1. ロールスロットツールチップを実装
1. サブロールを追加する
1. メインロールの種類を増やす
    * Miner, Warriorのロールスロットを見直し
    * Survivor, Builderを追加
1. サブロールの種類を増やす
1. 初期配布バンドル・シュルカーボックスを設定できるようにする
1. 経験値獲得の倍率・種類がロールによって変わるようにする
1. keepRoleGrowthmentオプションを追加する
1. メインロールの心得を追加する（コマンドで生成）
1. 心得の作成コスト見直し（クラフティングテーブルを利用）
1. 全プレイヤーロール一斉割り当ての管理者アクションを追加する
1. IntegratedとDedicatedでワールドオプションのデフォルト値が変わるようにする
    * 新規ワールドの時にデフォルト値適用
    * デフォルトに戻すコマンド実装
    * サーバーコンフィグファイル -> ゲームルール登録 -> ワールド作成時適用（バニラ実装）の方式が考えられる
1. ワールドの初期ロール割り当て方式オプションを変更する管理者アクションを追加
1. それ以外の管理者アクションを追加
1. ユーティリティの実装を検討する
1. 管理者アクションをGUI操作できるようにする。
1. RestrictedInventoryScreenのリファクタリング＆そのほかすべてのインベントリ画面の表示をカスタム
    * スロットバリアを削除する（インベントリ画面のカスタムで代替する）
    * 方式案: RestrictedInventoryScreenをリファクタリング＆チェストとかも共通処理化で見た目を制御
        * CreativeModeInventoryScreenを模倣したヘルパークラスを作成する作戦
            * CreativeModeInventoryScreenの完全コピー -> ヘルパーメソッド（public化）でアクセス可能に -> いらない処理を削る・カスタマイズ
            * AbstractContainerMenuの各画面継承をさらに継承したやつから上記ヘルパー関数を呼び出す
